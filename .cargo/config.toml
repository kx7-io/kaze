# Configuração do Cargo para otimizações e builds reprodutíveis.

[build]
rustflags = ["-C", "target-cpu=native"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"