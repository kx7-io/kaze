# Adicione esta linha ao seu ~/.config/hypr/hyprland.conf:
# exec-once = ~/kaze/target/release/kaze daemon

# Ou, se preferir usar o timer systemd em vez do daemon interno:
# exec-once = systemctl --user enable --now kaze-cleaner.timer